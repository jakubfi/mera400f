
# --- Project configuration -------------------------------------------------

PROJECT = mera400f
TOPLEVEL = mera400f
SOURCES_DIR = src
SOURCES = mera400f.v \
	decoder16.v decoder8.v \
	alu181.v carry182.v \
	dly.v \
	uart.v \
	pr.v regs.v r0.v rb.v bar.v ki.v l.v \
	pd.v ir.v idec1.v \
	px.v strobgen.v ifctl.v alarm.v \
	pm.v lk.v mc.v lg.v kcpc.v \
	pp.v rm.v rzrp.v dok.v \
	pa.v alu.v at.v ac.v ar.v ic.v w.v a.v \
	pk.v sevenseg.v display.v timer.v rot_dec.v \
	puks.v \
	isk.v \
	cpu.v \
	iobus.v msg_cmd.v msg_rx.v msg_tx.v \
	mem_elwro_sram.v memcfg.v \
	awp.v fps.v fpm.v fpa.v fic.v lp.v fpalu.v m.v t.v c.v k.v zp.v ld.v b.v expadd.v \
	fp_strobgen.v
SETTINGS = $(SOURCES_DIR)/settings.qsf
# See: https://github.com/jakubfi/altlogfilter (or comment out the line below)
ALTLOGFILTER = alf -c --

FAMILY = CycloneII
DEVICE = EP2C8Q208C8
PGM_CABLE = 1

FPGA_ROOT = ~/fpga
LIBS32_DIR = $(FPGA_ROOT)/lib32

# --- Quartus paths ------------------------------------------------------

ALTERA_ROOT = $(FPGA_ROOT)/altera/13.0sp1
Q_BINDIR = $(ALTERA_ROOT)/quartus/bin
LIB_PATHS = $(LIBS32_DIR):$(ALTERA_ROOT)/quartus/linux64:$(ALTERA_ROOT)/quartus/linux
Q_LIB_PREFIX = LD_LIBRARY_PATH=$(LIB_PATHS)

# --- Quartus tools ------------------------------------------------------

COMMON_ARGS = --no_banner
SETTINGS_ARGS = --write_settings_files=off --read_settings_files=on

Q_SH =  $(Q_LIB_PREFIX) $(Q_BINDIR)/quartus_sh
Q_MAP = $(Q_LIB_PREFIX) $(Q_BINDIR)/quartus_map $(COMMON_ARGS) $(SETTINGS_ARGS) --family=$(FAMILY)
Q_FIT = $(Q_LIB_PREFIX) $(Q_BINDIR)/quartus_fit $(COMMON_ARGS) $(SETTINGS_ARGS) --part=$(DEVICE)
Q_ASM = $(Q_LIB_PREFIX) $(Q_BINDIR)/quartus_asm $(COMMON_ARGS) $(SETTINGS_ARGS)
Q_STA = $(Q_LIB_PREFIX) $(Q_BINDIR)/quartus_sta $(COMMON_ARGS)
Q_PGM = $(Q_LIB_PREFIX) $(Q_BINDIR)/quartus_pgm $(COMMON_ARGS) -c $(PGM_CABLE)

# --- Files --------------------------------------------------------------

SRCS = $(addprefix $(SOURCES_DIR)/,$(SOURCES))

OUT_DIR = output_files
PROJECT_FILE = $(PROJECT).qpf
SETTINGS_FILE = $(PROJECT).qsf
SOURCES_FILE = $(PROJECT)_sources.qsf
MAP_OUTPUT = db/$(PROJECT).map.cdb
FIT_OUTPUT = db/$(PROJECT).cmp.cdb
SOF_OUTPUT = $(OUT_DIR)/$(PROJECT).sof
POF_OUTPUT = $(OUT_DIR)/$(PROJECT).pof

# --- Quartus targets ----------------------------------------------------

.phony: all test sum map mapsum fit fitsum asm install sta fmax jtag as clean distclean

all: map
map: $(MAP_OUTPUT)
sum: mapsum
mapsum: $(MAP_OUTPUT)
	cat $(OUT_DIR)/$(PROJECT).map.summary
fit: $(FIT_OUTPUT)
fitsum: $(FIT_OUTPUT)
	cat $(OUT_DIR)/$(PROJECT).fit.summary
asm: $(SOF_OUTPUT)
install: jtag

$(SETTINGS_FILE): Makefile
	$(ALTLOGFILTER) $(Q_SH) --prepare -f $(FAMILY) -d $(DEVICE) -t $(TOPLEVEL) $(PROJECT)
	$(ALTLOGFILTER) $(Q_SH) --set PROJECT_OUTPUT_DIRECTORY=$(OUT_DIR) $(PROJECT)
	@echo "" >> $(SETTINGS_FILE)
	@echo -e "source $(SETTINGS)" >> $(SETTINGS_FILE)
	@echo -e "source $(SOURCES_FILE)" >> $(SETTINGS_FILE)

$(SOURCES_FILE): $(SETTINGS_FILE)
	@echo -e "\n# ! WARNING ! this file is automaticaly generated by make, do not edit!\n" > $(SOURCES_FILE)
	@$(foreach var,$(SRCS),echo "set_global_assignment -name VERILOG_FILE $(var)" >> $(SOURCES_FILE);)

$(MAP_OUTPUT): $(SRCS) $(SOURCES_FILE)
	$(ALTLOGFILTER) $(Q_MAP) $(PROJECT)

$(FIT_OUTPUT): $(MAP_OUTPUT) $(SETTINGS)
	$(ALTLOGFILTER) $(Q_FIT) $(PROJECT)

$(SOF_OUTPUT): $(FIT_OUTPUT)
	$(ALTLOGFILTER) $(Q_ASM) $(PROJECT)

$(POF_OUTPUT): $(FIT_OUTPUT)
	$(ALTLOGFILTER) $(Q_ASM) $(PROJECT)

sta: $(FIT_OUTPUT)
	$(Q_STA) $(PROJECT)

fmax: $(FIT_OUTPUT)
	$(Q_STA) -t src/fmax.tcl

jtag: $(SOF_OUTPUT)
	$(Q_PGM) -m JTAG -o "p;$(SOF_OUTPUT)"

as: $(POF_OUTPUT)
	$(Q_PGM) -m AS -o "pv;$(POF_OUTPUT)"

# --- Cleanups -----------------------------------------------------------

clean:
	rm -rf db incremental_db $(OUT_DIR)

distclean: clean
	rm -rf $(PROJECT_FILE) $(SETTINGS_FILE) $(SOURCES_FILE)

